AI KNOWLEDGE STUDIO - COMPLETE TECHNICAL DOCUMENTATION
======================================================

1. OVERVIEW
-----------
AI Knowledge Studio is a Flask application for:
- audio/video transcription
- speaker diarization (pyannote default, NeMo optional)
- live chunk transcription
- transcript summarization (minutes format)
- document ingestion and document Q&A
- transcript Q&A
- transcript semantic search
- translation
- text-to-speech (gTTS + browser fallback)
- query-first agent routing
- plain chatbot interaction with short-term history
- deterministic runtime date/time and math responses

The current system is best described as:
- query-first
- tool-routed
- partially agentic

It is not yet a fully autonomous multi-step planning agent for every request, but it already exposes a unified agent query path and tool registry.


2. STARTUP FLOW
---------------
Startup sequence:
1. Flask app is created.
2. Workspace folders are ensured.
3. MongoDB init runs.
4. ASR model loads.
5. Diarization pipeline loads.
6. Routes are registered.
7. Web UI is served.


3. PRIMARY RUNTIME FLOWS
------------------------
3.1 Plain chat flow
- User types a query without uploaded file / transcript / document context.
- Frontend sends query to `POST /api/agent/query`.
- Router selects `chat_response`.
- `chat_response` passes recent `chat_history` into the LLM layer.
- Deterministic date/time and math handling run before the LLM fallback.
- Backend returns updated `history`.
- Frontend stores that history in `currentAgentChat`.

3.2 Upload + query flow
- User stages a file in the UI.
- No processing starts yet.
- User sends a query.
- Frontend sends file + query to `POST /api/agent/chat`.
- Backend detects media vs document upload.
- File is processed according to the query-driven agent path.

3.3 Transcript workflow
- media is processed
- transcript is generated
- speaker diarization runs
- transcript session is stored
- transcript can be summarized, searched, or used for transcript Q&A

3.4 Document workflow
- document text is extracted
- summary is generated
- text is chunked and embedded
- chunks are stored
- document Q&A uses retrieval + generation


4. PROJECT STRUCTURE
--------------------
main.py
- Flask app
- route definitions
- permission checks
- unified agent endpoints

app/agent_router.py
- query-first planning
- tool selection
- route result packaging

app/agent_tools.py
- tool registry
- tool metadata
- tool validation
- tool execution wrappers

app/agent_llm.py
- plain chatbot fallback
- deterministic date/time responses
- deterministic math responses using SymPy
- conversation-history-aware chat prompt
- planner LLM helper

app/agent_service.py
- uploaded file handling for query-driven agent flow

app/asr.py
- ASR loading and transcription logic

app/diarization.py
- diarization pipeline loading and execution

app/mapper.py
- speaker mapping / smoothing logic

app/media_utils.py
- media conversion / extraction / document text extraction helpers
- ensures audio is mono 16k for NeMo VAD stability
- deletes non-vocal Demucs stems by default

app/processing_service.py
- orchestration for media/document processing

app/summarize.py
- summary generation via Ollama or BART

app/translation.py
- NLLB translation loading and execution

app/tts.py
- gTTS synthesis (server)
- browser SpeechSynthesis fallback is used in the UI if gTTS fails

app/history_rag.py
- transcript-session RAG Q&A

app/document_rag.py
- document chunk retrieval + history-aware Q&A

app/semantic_search.py
- transcript semantic search

app/history_store.py
- transcript history persistence

app/document_store.py
- document metadata persistence

app/document_chunks.py
- document chunk persistence

templates/index.html
static/js/index.js
static/css/index.css
- docked assistant UI
- staged upload flow
- plain chat rendering
- transcript/document workflow rendering
- dark theme styling


5. AGENT DESIGN
---------------
Current tool registry includes:
- `chat_response`
- `process_media`
- `summarize_transcript`
- `translate_text`
- `answer_document`
- `answer_history`
- `search_history`
- `text_to_speech`

Routing behavior:
- explicit tool in payload is respected first
- otherwise planner / heuristic routing is used
- plain chat queries resolve to `chat_response`
- transcript/document context can shift the route to the matching RAG tools

Important limitation:
- the current router is still mostly single-tool-first routing
- there is limited chaining only in selected cases such as summary + translate


6. PLAIN CHAT MEMORY
--------------------
The normal chatbot keeps recent conversation history in the frontend.

Frontend:
- `currentAgentChat` stores the current chat thread
- recent turns are sent in `payload.chat_history`

Backend:
- `chat_response_tool()` appends the current turn and returns updated history
- `chat_with_agent()` includes `Conversation History` in the LLM prompt

Scope:
- memory is session-local in the current browser session
- it is reset when transcript/document workflows become active
- it is not yet persisted as a long-term standalone chat history collection


7. DETERMINISTIC UTILITY LAYER
------------------------------
The normal chatbot has a pre-LLM deterministic layer for:

7.1 Date / time
- current day
- current date
- current time

7.2 Math
- parsing arithmetic and basic algebra
- solving quadratic equations


8. DIARIZATION OPTIONS
----------------------
Default diarization uses pyannote.

Optional NeMo diarization:
- Configure with `configs/nemo_diarization.yaml`.
- Requires NeMo dependencies.
- Uses VAD + embedding + clustering.


9. EXPORT FORMAT
----------------
- Summary and transcript exports are **PDF**.
- Exports follow a **Minutes of a Meeting** structure with bold headings and bullet points.


10. TEXT-TO-SPEECH
------------------
- Server-side TTS uses gTTS.
- gTTS requires internet access.
- If gTTS fails, the UI uses browser SpeechSynthesis as fallback.


11. UI BEHAVIOR NOTES
---------------------
- New Session refreshes the app and returns to home without logging out.
- Hard reload (Ctrl+Shift+R) logs out by design.
- Q&A responses render inside the Q&A panel.
- Summary button remains dedicated to summary generation.


12. TROUBLESHOOTING
-------------------
- MongoDB connection issues:
  - ensure MongoDB is running
  - or set `MONGODB_URI`

- Ollama-backed chat / summary / RAG not responding:
  - verify Ollama is running
  - verify `OLLAMA_URL`
  - verify the configured model exists locally

- NLLB model not found:
  - set `NLLB_MODEL_PATH`
  - or place the local model in expected directories

- Diarization model not loading:
  - set `HUGGINGFACE_TOKEN`

- NeMo VAD error about tensor dimensions:
  - ensure audio is mono 16k; the pipeline forces mono conversion

- Live transcription not working:
  - allow microphone permission in the browser
  - check that the browser supports `MediaRecorder`

- Scanned PDF returns no text:
  - enable OCR with `DOCUMENT_OCR=1`
  - install `pdf2image`, `pytesseract`, and system Tesseract
